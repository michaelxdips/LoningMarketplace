# V1.5 Location — Verification Checkpoint

## Phase 1 — Reconstruction & Preflight (DONE)

- Branch: `phase1-public-discovery`, HEAD: `93311cc` (Phase 0 baseline is HEAD, ancestor check OK).
- Worktree clean; tags `v1.4.0-preview.1`, `v1.4.0-preview.2` exist.
- Historical migrations 0005–0009: no working-tree diff.
- Journal: entries idx 0–9, last `0009_repair_public_integrity` (idx 9, when 1785465960000).
- `Simulasi Lokasi Usaha` renders only in `frontend/src/components/shared/UMKMDetailDialog.tsx:172`.
- `address` is the only textual address contract (`umkms.address` text NOT NULL; `UMKM.address`, `umkmInput.address`).
- Roles from source: `superadmin|admin|perangkat_desa|pelaku_umkm` (`backend/src/auth/policy.ts` enum); guards: `authenticate/origin/csrf/admin/manager`, `managerSecured`, `adminSecured` (`backend/src/auth/guards.ts`).
- Ownership: `loadUMKM` in `backend/src/routes/manage.ts:12` — admin OR `item.ownerUserId === auth.user.id`, else 403 FORBIDDEN; anonymous → 401 UNAUTHENTICATED.
- Migration runner: `backend/src/db/migrate.ts` — prepare → migrate slice → assertPublicIntegrity. Integrity module: `backend/src/db/public-integrity.ts` (`collectPublicIntegrityFailures`, `assertPublicIntegrity`).
- DB audit: `backend/src/scripts/db-audit.ts` uses `collectPublicIntegrityFailures`.
- Public serialization: `mapUMKM` in `backend/src/db/repository.ts:29`; managed: `mapManagedUMKM` (spread row).
- SEO/JSON-LD owner: `frontend/src/lib/seo.ts` + `frontend/src/pages/UMKMDetailPage.tsx:29` (LocalBusiness).
- CSP owner: `@fastify/helmet` default in `backend/src/app.ts:34` (no explicit CSP config yet — helmet default CSP has `frame-src 'self'`).
- E2E fixture owner: `backend/src/scripts/e2e-setup.ts` + `e2e/support/fixtures.ts`.
- Frontend data flow: `frontend/src/lib/management.ts` (managementApi), `frontend/src/hooks/useManagement.ts` (query keys `['manage', key, ...]`, public keys `['umkm', id]`, `['umkms', ...]`).
- Dashboard routes: `frontend/src/main.tsx:94-109` (DashboardShell children).
- Isolated harness: `scripts/run-isolated.mjs` (modes: integration|e2e|full|migration|zoom-native), disposable compose projects, DB `loning_*`.
- Phase 0 frozen files (do not touch): `frontend/vite.config.ts`, `backend/tests/migration-0008-contract.test.ts`.

## Step 0 — Actual State Reconstruction (DONE)

- `git status --short`: only V1.5 files changed/untracked. No `backend/backend` stray exists. `git diff --check` clean.
- Phase 0 files unchanged: `frontend/vite.config.ts`, `backend/tests/migration-0008-contract.test.ts` — no diff.
- Historical migrations 0005–0009 — no diff.
- All reported Phase 2/3 files confirmed present at correct paths with expected content.

## Phase 2–4 — Evidence (DONE, unit level)

- `0010_umkm_business_location.sql`: 2 columns nullable `numeric(9,6)`, 3 exact constraints, no backfill/DML.
- Journal: `0010_umkm_business_location` at idx 10 after `0009`.
- `schema.ts`: `numeric('latitude',{precision:9,scale:6})` + 3 named checks on `umkms`.
- `src/domain/location.ts`: `normalizeCoordinates` (round half-up 6dp, -0→0, ranges), `parsePgNumeric` (safe numeric-string→number, empty→null).
- `src/db/location-integrity.ts`: `collectLocationIntegrityFailures` (partial pairs, ranges, column types, constraint presence+validated+`pg_get_constraintdef` LIKE checks), `assertBusinessLocationIntegrity`.
- `src/db/migrate.ts`: order prepare → migrations through 0010 → `assertPublicIntegrity` → `assertBusinessLocationIntegrity`.
- `src/scripts/db-audit.ts`: extended with `collectLocationIntegrityFailures` (no new audit command).
- Tests: migration-0010 (10) + migration-0008 (5) + location domain (32) + location-integrity (4) pass; backend full suite 150/150; backend typecheck clean.

## Phase 2 — Runtime matrix (PENDING)

## Migration runtime matrix — PASS

- Command: `npm run test:migration:existing:isolated` — exit 0 after ledger-aware migration-runner fix.
- Fresh migration, existing legacy rows, preparation failure/recovery, duplicate slug refusals, invalid phone refusal, and idempotent rerun executed by repository-native harness.
- Final audit: UMKMs 4, Products 4, Published 2, Draft 2; all integrity counters 0; `Audit Status: PASS`; `EXISTING_DATA_MIGRATION_PASS`.
- Migration runner applies missing ledger entries transactionally, including rewound 0008/0009 fixtures; records hash/timestamp only after SQL succeeds.

## Phase 5–10 — Local evidence

- Backend location API: PATCH/DELETE `/api/manage/umkms/:id/location`; strict object; normalized numeric-only payload; owner/admin authorization; audit actions `umkm.location_updated` / `umkm.location_cleared`; catalog timestamp touched.
- Backend tests: 165/165 pass before CSP assertion; targeted routes after CSP: 19/19 pass.
- Frontend location parsing and URL generation: 37/37 focused tests; supported direct pair, Google `@`/query, OSM hash/marker; no redirect/fetch; short/insecure/unrelated/hostile inputs rejected.
- Public `BusinessLocation`: omission without pair, internal OSM iframe, attribution, Google search/directions links; 6/6 focused tests.
- Frontend full suite after map component: 116/116; after JSON-LD test: 118/118.
- JSON-LD: normalized `GeoCoordinates` only for valid pair; 2/2 focused tests.
- CSP: exact `frame-src 'self' https://www.openstreetmap.org`; no wildcard/Google frame source; tested in backend route suite.
- Dashboard: `/dashboard/umkms/:id/location`; `/dashboard/location` aliases to managed UMKM selection. URL parse never auto-saves; manual fields, preview, normalized dirty state, save/remove confirmation, success/error/focus handling implemented.
- `Simulasi Lokasi Usaha` removed and replaced in dialog; public detail page receives same component.

## Static gates

- Backend unit tests: 165/165 pass.
- Frontend unit tests: 118/118 pass.
- Full typecheck: pass.
- Lint: pass.
- Configured production build: pass (`VITE_PUBLIC_SITE_URL=https://site.example.invalid`, `VITE_API_URL=https://api.example.invalid/api`).
- Harness safety: 3 valid accepted, 19 unsafe refused.
- `git diff --check`: pass.
- Full isolated E2E and native zoom: pending clean serial rerun (first parallel attempt invalidated by shared port/project collision).
